package lesson7;

/*public final class Sort extends ArrayPI21 {
    //ArrayPI21 arr = new ArrayPI21();

    public void sorting() {
        boolean isSorted = false;
        while (!isSorted) {
            isSorted = true;
            for (int i = 1; i < arr.length; i++) {
                if (arr[i - 1] > arr[i]) {
                    int tmp = arr[i];
                    arr[i] = arr[i - 1];
                    arr[i - 1] = tmp;
                    isSorted = false;
                }
            }
        }
        for(int i = 0; i<arr.length; i++){
            System.out.print(arr[i] + " ");
        }
    }
    public static void main(String[] args){
        Sort s = new Sort();
        //s.inarr();
        //s.sorting();
    }
}*/
